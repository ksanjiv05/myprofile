# Caddy alternative — automatic HTTPS, one binary, ~20MB RSS.
#   sudo cp deploy/Caddyfile /etc/caddy/Caddyfile
#   sudo rsync -a --delete public/ /var/www/myprofile/
#   sudo systemctl reload caddy

imsanjiv.in, www.imsanjiv.in {
    root * /var/www/myprofile
    encode zstd gzip
    try_files {path} /index.html
    file_server

    header /assets/* Cache-Control "public, max-age=3600, must-revalidate"
    header {
        X-Content-Type-Options nosniff
        Referrer-Policy strict-origin-when-cross-origin
        -Server
    }
}
